# ReactWebApp
During a web development course from my bachelor's, I built a web application built using React for frontend and node.js for the backend, in which a phone processors inventory is being stored, together with the phones that are built with a specific processor inside.
